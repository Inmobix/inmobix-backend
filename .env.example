# ============================================
# CONFIGURACIÓN PARA PRODUCCIÓN (Railway/Supabase)
# ============================================
# Copia este archivo a .env y configura tus valores

# Base de datos (Railway/Supabase)
DB_URL=jdbc:postgresql://tu-host.railway.app:5432/railway
DB_USER=postgres
DB_PASSWORD=tu-password-aqui

# Postmark Email Service
POSTMARK_API_TOKEN=tu-token-postmark
POSTMARK_FROM_EMAIL=tu-email@ejemplo.com
POSTMARK_FROM_NAME=Inmobix

# URLs
BACKEND_URL=https://tu-backend.railway.app
FRONTEND_URL=https://tu-frontend.vercel.app

# CORS
CORS_ALLOWED_ORIGINS=https://tu-frontend.vercel.app,http://localhost:4200
CORS_ALLOW_CREDENTIALS=true

# Puerto (Railway lo asigna automáticamente)
PORT=8080

# ============================================
# NOTA: Para desarrollo local, usa el perfil 'dev'
# ejecutando: mvn spring-boot:run -Dspring-boot.run.profiles=dev
# o configurando: SPRING_PROFILES_ACTIVE=dev
# ============================================
